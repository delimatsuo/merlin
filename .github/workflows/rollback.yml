# Generated by /deploy-setup — starter template, review TODOs before use
name: Rollback

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to rollback'
        required: true
        type: choice
        options: [staging, production]
      target:
        description: 'What to rollback'
        required: true
        type: choice
        options: [frontend, backend, both]
      method:
        description: 'Rollback method'
        required: true
        type: choice
        options: [traffic-split, git-revert, redeploy-tag]
      revision_or_tag:
        description: 'Cloud Run revision or git tag'
        required: true
        type: string

permissions:
  id-token: write
  contents: read

env:
  GCP_PROJECT_ID: merlin-489714
  CLOUD_RUN_SERVICE: merlin-backend
  CLOUD_RUN_REGION: southamerica-east1

jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      # TODO: Configure authentication (same as deploy.yml)

      # TODO: Implement rollback logic
      # For Cloud Run traffic split:
      # - name: Rollback Cloud Run
      #   if: inputs.target == 'backend' || inputs.target == 'both'
      #   run: |
      #     gcloud run services update-traffic $CLOUD_RUN_SERVICE \
      #       --to-revisions=${{ inputs.revision_or_tag }}=100 \
      #       --region $CLOUD_RUN_REGION \
      #       --project $GCP_PROJECT_ID

      # For Firebase Hosting rollback:
      # - name: Rollback Firebase Hosting
      #   if: inputs.target == 'frontend' || inputs.target == 'both'
      #   run: |
      #     firebase hosting:rollback --project $GCP_PROJECT_ID

      - name: Rollback placeholder
        run: echo "TODO - Configure rollback steps for ${{ inputs.environment }} (${{ inputs.target }}) using ${{ inputs.method }}"
